# Astra automation handoff — run 123

Problem 1 remains OPEN.

Starting branch tip: `a2c90622432dc372ae310bb53cb836a404f811d3` (`research/astra-next`). No intervening work was found after run122.

## Result / blocker refinement

Audited whether the stronger facts in the established two-bit nonreset return can upgrade run122's actual-ancestry route into a finite birth budget. They cannot from the currently pushed identities.

The passage forces beta=1 at t+6, makes that new one-bit t source resetting, and excludes another nonresetting source through t+7. But none of those statements supplies an update law for a finite label tied to the original actual row. Actual ancestry is indefinitely reusable by left-permutivity; shadow ancestry draws from an infinite initial shadow/disagreement supply; and "resetting" has not been proved to consume an original-support label.

The exact missing lemma is now stated explicitly: find a finite label set S determined by the original actual row and a charge from sufficiently late forced births to S with uniformly bounded multiplicity (injection as the strongest case), or an equivalent irreversible/monotone finite resource. Without such a cross-episode update theorem, further local forcing-cone expansion or fixed-window phase enumeration cannot close the birth budget.

Recorded in `proofs/informal/problem1_birth_budget_requires_reuse_theorem_not_ancestry.md`, research commit `bf2de13c3e9c750ffc1c256dbdb8c64e43078de0`.

## Next target

Search specifically for a cross-episode invariant tied to the finite ACTUAL row, and prove its transition across both resetting and nonresetting passages. Do not repeat ancestry-only charging or enlarge the already-collapsed local 001 forcing cone unless it yields that transition law.
